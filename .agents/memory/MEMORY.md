# Team memory

One line per memory. Read this at the start of every session; open a file
only when its hook is relevant.

- [Fix deprecations in touched code](fix-deprecations-in-touched-code.md) — deprecated API on a line being edited is in scope, and `-q` hides the warnings that reveal it.

# Team memory

One line per memory. Read this at the start of every session; open a file
only when its hook is relevant.

- [Own the deprecations your change introduces](own-deprecations-your-change-introduces.md) — a bump deprecates untouched code too; fix mechanical, park semantic, and never use `-q`.
